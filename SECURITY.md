# 安全策略

## 报告漏洞

请使用 GitHub 仓库的私密 Security Advisory 报告安全问题。漏洞修复发布前，不要创建公开 Issue，也不要公开利用代码、真实凭据或受影响部署的信息。

报告应包含：

- 受影响版本或提交；
- 可重复的最小复现步骤；
- 预期影响和攻击前提；
- 建议修复方向；
- 已删除凭据和个人信息的必要日志。

## 凭据边界

- `config.yaml`、`secrets.env`、数据库和工作目录不得提交。
- GitHub、GitCode、飞书和模型服务凭据只在服务端保存。
- 平台 Token 不传给 Codex，不写入 Git 命令参数或任务结果。
- 网页保存的集成凭据使用本地加密主密钥加密。
- 发现凭据泄漏后应立即撤销并轮换，不要只删除 Git 工作树中的文件。

## 执行风险

Issue Agent 会在独立工作目录中运行仓库依赖安装和测试，默认允许联网。管理员只应授权可信公共仓库，并使用独立服务账号、最小平台权限和操作系统级隔离。PR 静态检视不安装依赖、不运行仓库脚本，也不修改代码。

## 公网部署

Manager 和模型代理必须监听回环地址。公网访问应通过 HTTPS 反向代理提供，只开放 80/443，不暴露 SQLite、工作目录、日志、内部端口或容器管理接口。
